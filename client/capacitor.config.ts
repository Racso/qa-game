import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.racso.qagame',
  appName: 'QA Game',
  webDir: 'dist/qa-game/browser'
};

export default config;
